import { createStore } from "vuex";
// import RecipeDataService from "../services/RecipeDataService";

export default createStore({
  // state() {
  //   return {
  //     currentRecipeView: {},
  //     littleBarBookRecipes: {},
  //   };
  // },
  // mutations: {
  //   SET_currentRecipeView(state, id) {
  //     RecipeDataService.get(id).then(
  //       (res) => (state.currentRecipeView = res.data)
  //     );
  //   },
  // },
  // actions: {
  //   setCurrentRecipeView({ commit }, id) {
  //     commit("SET_currentRecipeView", id);
  //   },
  // },
  // getters: {
  //   getCurrentRecipeView(state) {
  //     return state.currentRecipeView;
  //   },
  // },
  // modules: {},
});
